<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Màu nền -->
    <solid android:color="#1976D2"/>

    <!-- Bo góc nhẹ kiểu vuông đẹp -->
    <corners android:radius="6dp"/>

    <!-- Viền -->
    <stroke
        android:width="1dp"
        android:color="#1565C0"/>

    <!-- Padding -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/>
</shape>